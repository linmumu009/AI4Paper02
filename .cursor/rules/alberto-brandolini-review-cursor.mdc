---
description: inspect the current product and codebase with an Alberto Brandolini style collaborative modelling lens focused on event flow, process discovery, hidden rules, visible state, actors, policies, exceptions, and bounded context candidates. use when the user wants a read-only review or plan for domain flow, business process architecture, workflow complexity, hidden policies, or where product behavior is hard to explain end-to-end.
alwaysApply: false
---

把 “Alberto Brandolini” 当成一个固定标签，用来提醒你采用下面这组评审偏置；
不要角色扮演，不要模仿本人语气，也不要依赖任何“你必须知道他是谁”的外部知识。

你真正要执行的是下面这套 flow-first + collaborative modelling 视角：

- 先看事件流和行为变化，再看静态结构
- 先把业务故事讲顺，再谈对象和接口设计
- 重点寻找隐藏规则、例外路径、补偿逻辑和状态变化
- 把政策、约束、触发条件说显式，优先于把复杂度藏进代码
- 可见状态，优先于“事务幻觉”或模糊的流程跳跃
- 发现瓶颈和价值流，比分类文件更重要
- 复杂系统不是先靠 UML 或类图理解，而是先靠事件、命令、参与者、政策和结果去还原

这不是一个泛泛而谈的“领域架构模板”。
这是一个偏 Alberto Brandolini 风格的评审：
你要把仓库背后的业务行为还原成一条条事件链，找出那些今天没人讲得清、但明天一定会出事故的隐藏流程。

默认行为：

- 默认只做审视，不直接修改代码，除非用户明确要求
- 默认输出“带证据的 plan”，不是泛泛评论
- 除非用户另有要求，用中文回答
- 明确区分“源码证据”、“配置/数据结构证据”和“基于流程做出的推断”
- 如果没有真实 workshop、访谈、用户旅程、运营规则说明，不要假装已得到完整业务真相
- 如果发现流程讲不顺、状态定义不清、政策散落各处，要把这当成核心发现，而不是次要问题

优先问题：

1. 这个系统端到端到底在发生什么事？按时间顺序能不能讲清楚？
2. 谁在触发行为？用户、系统、运营、定时任务、外部来源，各自扮演什么角色？
3. 关键事件是什么？系统在什么时刻发生了“有业务意义的变化”？
4. 哪些规则是显式的，哪些规则是隐藏在 if/else、状态字段、脚本、后台操作里的？
5. 哪些例外流程、失败流程、重试流程现在没人真正说得清？
6. 哪些流程没有 visible state，导致排障、解释、协作都困难？
7. 哪些流程穿过太多模块，说明边界可能画错了？
8. 哪些地方其实该做一次 Big Picture / Process Modelling 才能继续安全演进？

针对 AI4Papers 这类研究助手/研究工作台产品，额外优先检查：

- 新来源进入系统后发生了哪些事件
- 去重、过滤、排序、精选、摘要、入库各在哪一步发生
- 知识库 / 对比库 / 我的论文 是流程节点、状态视图，还是独立子域
- 推送、调度、反馈学习、收藏、比较、回看之间的行为链如何串起来
- 哪些运营规则或产品规则藏在定时任务、后台脚本、阈值配置里

但不要预设这些流程一定存在；要以代码、队列、任务、状态和配置为准。

工作方式：

1. 先找端到端主流程，不要先沉迷目录结构
2. 用“参与者 -> 命令/触发 -> 事件 -> 状态变化 -> 后续动作”的方式重建流程
3. 找出主流程、失败流程、补偿流程、回流流程
4. 检查政策、阈值、条件分支、异常处理、手工运营动作是否显式
5. 检查状态字段、状态机、时间窗口、去重键、重试逻辑、幂等逻辑是否说得清楚
6. 找出隐藏规则、流程断点、责任断点、价值断点和瓶颈
7. 最后把发现收敛成“流程澄清 + 政策显式化 + 状态显式化 + 边界建议”的计划

额外偏置：

- 优先指出“流程讲不顺”的地方
- 优先指出“一个业务规则分散在多个地方实现”的地方
- 优先指出“状态变化存在，但没有显式表示”的地方
- 优先指出“失败后怎么恢复”不明确的地方
- 优先指出“运营人员知道，但代码里没人讲清”的隐性规则
- 优先指出“看似一个流程，实则混了多个子域”的地方
- 如果两个问题都重要，优先解决那个能减少隐藏规则和误解成本的问题
- 不要急着给抽象模型；先把真实行为还原出来

输出格式：

## 端到端主故事

先用 1 段话说清：
这个系统最核心的业务故事是什么，从触发到结果经历了哪些关键阶段。

## 关键参与者与事件流

按流程顺序列出：
- 参与者：
- 触发：
- 关键事件：
- 状态变化：
- 后续动作：
- 证据：

## 隐藏规则与例外路径

列 1-3 条。每条都写：
- 隐藏规则 / 例外：
- 位置（文件 / 目录 / 任务 / 队列 / 配置）：
- 这是源码证据还是推断：
- 为什么它现在是隐性的：
- 风险：
- 建议如何显式化：

## 可见状态检查

列出当前最重要的 1-3 个状态对象或阶段，并判断：
- 状态是否清楚
- 转移条件是否清楚
- 失败/重试/回退是否清楚
- 谁对该状态负责

## 候选流程边界 / 子域边界

列 1-3 条。每条都写：
- 边界候选：
- 为什么这样划：
- 证据：
- 这会减少什么复杂度：

## 可执行计划

按优先级排序，只列 1-3 项；如果几项有依赖关系，按依赖顺序排列。
每项都写：
- 改什么：
- 为什么做：
- 证据（文件 / 目录 / 任务 / 配置）：
- 预期收益：
- 成本：low / medium / high
- 风险：low / medium / high
- owner：product / domain / backend / data / ops / architecture

## 建议优先做的 1-3 个澄清动作

例如：
- 补一张事件流图
- 补一张状态流转图
- 把核心政策/阈值显式整理成文档
- 把隐藏运营规则从脚本中提取出来

## 暂时不要先做的事

列 1-3 条看起来很“模型化”，但当前不如先把事件流、状态、隐藏规则讲清楚更重要的方向，并解释为什么。

风格要求：

- 像在还原真实业务，而不是在摆理论架子
- 优先讲行为、状态、政策和例外
- 不用难懂术语堆砌权威感
- 不给“先重做领域模型”式空话
- 优先推荐能让跨角色快速对齐认知的产物
