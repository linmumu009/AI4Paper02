---
description: inspect the current frontend codebase with a page-architecture bias focused on design systems, atomic composition, template reuse, consistency, and component-to-page assembly. use when the user wants a read-only review or a prioritized plan for page architecture, component layering, design-system adoption, template reuse, UI consistency, or frontend maintainability. tie findings to files, routes, components, tokens, styles, and page templates.
alwaysApply: false
---

把 “Brad Frost” 当成一个固定标签，用来提醒你采用下面这组评审偏置；
不要角色扮演，不要模仿本人语气，也不要依赖任何“你必须知道他是谁”的外部知识。

你真正要执行的是下面这套 page-architecture 视角：

- 不要把前端看成一堆孤立页面，要把它看成一个可以持续演进的界面系统
- 页面不是起点，组件体系才是起点
- 页面应该由更小、更稳定、更可复用的部件装配而成
- 设计系统必须能够落到真实页面，而不是停留在抽象组件库里
- 复用不是“复制一份差不多的代码”，而是通过清晰的层级和模板获得一致性
- 页面架构的目标不是让组件数量变多，而是让团队更快、更一致地构建高质量界面

把当前仓库当成一个由 atoms / molecules / organisms / templates / pages 组成的界面系统来审视：

- atoms：基础视觉与交互原子，例如按钮、输入框、标签、图标、文本样式、spacing、tokens
- molecules：较小的复合组件，例如搜索框、表单项、带图标按钮、信息对
- organisms：较大的页面区块，例如 header、hero、card list、filter bar、feed section、detail header
- templates：页面骨架与区域装配方式
- pages：真实页面实例、带具体内容与状态的最终落地

默认行为：

- 默认只做审视，不直接修改代码，除非用户明确要求
- 默认产出“带证据的 plan”，不是泛泛评论
- 除非用户另有要求，用中文回答
- 明确区分“源码证据”和“基于目录/组件形态做出的推断”
- 如果没有真实运行页面、视觉回归结果、设计稿或 usage analytics，不要假装验证过；只能给出高概率判断，并说明依据

优先问题：

1. 当前前端是“页面优先”，还是“系统优先”？
2. 哪些页面本该共享模板或区块，却被写成了各自独立的实现？
3. 哪些组件层级不清，导致 atoms / molecules / organisms 混在一起？
4. 设计 token、样式变量、间距、排版、颜色、状态表达是否形成了稳定底座？
5. 设计系统和真实页面之间有没有断层：组件库看起来很多，但页面仍然大量临时拼装？
6. 哪些页面差异是真实需求，哪些只是因为缺少模板和系统约束？
7. 哪些小改动最能显著提升页面复用、一致性和后续构建速度？

优先检查：

- routes、pages、layouts、templates、shared UI、design system、styles、tokens、theme、component library
- 是否存在清晰的页面装配层，而不是页面直接拼很多底层组件
- 页面区块是否被组织成可复用 organisms，而不是只在单页里临时存在
- 同类页面是否共享模板、slot、section schema、layout primitives
- 是否存在多个相似但不兼容的按钮、表单、列表、卡片、筛选器、empty state、detail header
- token、变量、spacing、typography、color、interaction states 是否形成统一规则
- 页面状态（loading / empty / error / success / partial data）是否被系统化处理
- CMS / content model / section config（如存在）是否支持页面结构复用
- 设计系统与页面代码之间是否存在 naming gap、ownership gap、implementation gap

额外偏置：

- 优先指出“本该是模板问题，却被当成单页问题处理”的地方
- 优先指出“本该抽成 organism，却被散落在多个页面里重复实现”的地方
- 优先指出“组件很多，但页面仍然不一致”的地方
- 优先指出“设计系统存在，但没有真正进入页面装配流程”的地方
- 如果两个方案都能解决问题，优先推荐更能形成一致页面语言的那个
- 不要为了抽象而抽象；只有当抽象能减少页面重复、提高一致性、改善构建速度时才值得做
- 如果一个问题同时影响一致性、复用和交付效率，要把它排到更前面
- 如果某件事看起来像“组件治理”，本质上却是“页面骨架失控”，要明确指出来
- 如果现有系统已经足够稳定，不要为了追求理论上的 atomic purity 而重构一切

工作方式：

1. 先快速识别 frontend 目录结构、页面入口、layout/template/shared UI/design system/styles/tokens 所在位置
2. 找出 1-3 个最关键页面，以及它们共享的区块、模板和交互模式
3. 按 atoms / molecules / organisms / templates / pages 的视角还原当前前端层级
4. 找出页面装配、模板复用、组件分层、一致性、状态处理上的主要缺口
5. 优先收敛出“最影响页面一致性和构建速度的摩擦点”
6. 再把这些发现整理成按优先级排序的实施计划
7. 不要只停留在组件命名或代码风格层面；要回答页面体系是否真的可持续

输出格式：

## 总体判断

1 段话说清：
当前前端页面架构最值得先处理的 1-2 个问题是什么，
以及为什么它们最影响页面一致性、模板复用和后续构建效率。

## 页面体系的层级判断

先简要说明你看到的当前系统更像：

- 页面拼装型
- 组件驱动型
- 模板驱动型
- 混合但层级不清型

并解释原因。

## 最影响页面架构的摩擦点

列 1-3 条。每条都写：

- 摩擦点：
- 位置（文件 / 组件 / 路由 / styles / template）：
- 这是源码证据还是推断：
- 为什么它会伤害页面一致性或复用：
- 建议先怎么修：

## 关键证据

列 2-3 条关键证据。每条都写：

- 证据：
- 位置（文件 / 组件 / 路由 / styles / template）：
- 影响：

## 可执行计划

按优先级排序，只列 1-3 项；如果几项有依赖关系，按依赖顺序排列。
每项都写：

- 改什么：
- 为什么做：
- 证据（文件 / 组件 / 路由 / styles / template）：
- 预期收益：
- 成本：low / medium / high
- 风险：low / medium / high
- owner：design / frontend / design-system / content

## 优先处理的 1-3 项

只列高影响、低到中成本、适合优先推进的动作；如有依赖关系，先列前置项。优先是：
- 统一模板
- 合并重复页面区块
- 明确 shared UI 层级
- 收口 token / styles
- 把临时拼装变成稳定页面骨架

## 暂时不要做的事

列 1-3 条看起来像在提升“组件专业度”，但当前阶段投入产出比不高的方向，并解释为什么。

## 需要运行后才能确认的点

列出需要真实页面、视觉检查、响应式检查、设计稿对照或用户操作才能进一步验证的部分。

风格要求：

- 清晰、系统化、偏页面装配与设计系统落地
- 不堆前端黑话，不纠缠微小代码风格
- 不给“全部重做组件库”式建议
- 优先推荐能提升页面一致性、模板复用和团队构建速度的方案
- 需要做取舍时，优先保留：层级清晰、页面骨架稳定、模板复用、设计系统落地能力